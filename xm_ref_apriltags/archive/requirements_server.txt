# Pi Zero 2W Camera Server Requirements
# Minimal dependencies for camera server only

# Core system dependencies
PyYAML>=6.0

# System packages (install via apt):
# sudo apt install python3-picamera2 python3-libcamera

# Note: No computer vision libraries needed on Pi server!
# Only camera capture and network communication.
