# Requirements for Pi Zero 2W Camera Server System
# Install with: pip install -r requirements.txt

# Essential packages for camera server
PyYAML>=6.0
python-dotenv>=1.0.0

# Camera support (install via pip, fallback to system package)
picamera2>=0.3.12

# Required for solid_dosing_server imports
# socket, threading, datetime, pathlib, time - all built-in to Python

# Basic utilities
pathlib2>=2.3.7; python_version<"3.4"

# Optional: Lightweight packages
coloredlogs>=15.0

# AprilTag detection (optional, may fail on Pi Zero 2W due to memory)
# pupil-apriltags>=1.0.4  # Commented out due to compilation issues

# Heavy packages (install manually if needed)
# numpy>=1.24.0
# matplotlib>=3.7.0  
# scipy>=1.10.0

# Note: Install these via system packages to avoid compilation:
# sudo apt install python3-numpy python3-matplotlib python3-scipy
# sudo apt install python3-opencv python3-picamera2 (if available)
